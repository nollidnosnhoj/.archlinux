#!/usr/bin/env bash

if ! command -v stow &> /dev/null; then
    sudo pacman -S --needed --noconfirm stow
fi

pushd $HOME/.archlinux/dotfiles > /dev/null
stow --dir=$HOME/.archlinux/dotfiles --target=$HOME */
popd > /dev/null